name: Build & Test

on:
  pull_request:
    branches:
      - 'master'
  push:
    branches:
      - 'golang-dev'

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-haskell@v1.1
        with:
          ghc-version: '8.10.7' 
          enable-stack: true
          stack-version: 'latest'
      - run: stack build
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-haskell@v1.1
        with:
          ghc-version: '8.10.7' 
          enable-stack: true
          stack-version: 'latest'
      - run: ./demos/runTest

