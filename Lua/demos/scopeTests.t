Variable scope tests (local)

  $ ./demoLocal.exe
  { vars = [["x" -> (VNumber 5.)
    
  ]]
  ; last_value = VNull; prev_env = None; is_func = false; is_loop = false;
  jump_stmt = Default; last_env = None
  }
